#language-bar
  %ul.right
    %li #{t 'header.change_country'}
    %li.has-dropdown
      %a(data-dropdown='lang-dropdown' data-options='is_hover:true') #{ t :region}
      .f-dropdown#lang-dropdown(data-dropdown-content)
        .inner
          - regions.each do |region|
            .lang-list
              %h5= region.name
              %ul
                - region.countries.each do |country|
                  %li
                    = link_to country.name, "/#{country.locale}"



    %li
      %a.ignore{:href=>'#'} #{t 'header.ignore'}
  %p #{ t 'header.language_notification', :country => 'Iceland' }