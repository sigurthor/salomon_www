#language-bar
  %ul.right
    %li Change country
    %li.has-dropdown
      %a(data-dropdown='lang-dropdown' data-options='is_hover:true') USA / English
      .f-dropdown#lang-dropdown(data-dropdown-content)
        .inner
          - regions.each do |region|
            .lang-list
              %h5= region.name
              %ul
                - region.countries.each do |country|
                  %li
                    = link_to country.name, "/#{country.locale}"



    %li
      %a.ignore{:href=>'#'} Ignore
  %p Hi there, you seem to be visiting from #{t :region}. Do you want to change the location and language?